*k8s : containers orchestration 
-deploy containers applications ( apps medvlopin b containers)
-distribute load al multiple servers
-auto scale 
-monitor health check lel containers
-replacement of failed containers

=> k8s deploys containerized apps

*pod : asgher unit nhoto feha el container(s) 
-pod feha volumes w ip addresse w containers

*node : fih el pods => el howa server 
-kol node fih kubelet kube-proxy w container runtime (eli tkhadem fih app containers)
kubelet : howa el responsible al star stop w restart mta pods , monitors health mta pods   of the node , interacts with api server , manages containers of pods

*k8s cluster : set of nodes(server)

-el cluster fih master node w worker node
-el masternode howa fih config w howa yamel  distribution mta load al 
worker nodes. fih zeda api server ( point d'entrées mta les request lkol men users l kubctl) , scheduler (planning and distrubtion of load ) , kube controller manager (controls what happens on each node)
cloud controller manager (communicate with cloud provider services),
etcd (logs related to cluster stored as key value)

*kubectl => howa clt to manage kubenetes cluster (interact with api server of master node)